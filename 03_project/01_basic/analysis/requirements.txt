# Dependencies for the metric computation and analysis code.
# Pinned to the `qrd-metrics` conda environment (Python 3.10.19) that computed
# the metrics AND generated the figures/statistics reported in the paper.

# Core numerical / data
numpy==2.1.2
scipy==1.15.3
pandas==2.3.3

# Plotting
matplotlib==3.10.7
seaborn==0.13.2
Pillow==12.0.0

# Deep-learning metrics
torch==2.9.1
torchvision==0.24.1
lpips==0.1.4
pytorch-msssim==1.0.0

# OpenAI CLIP (ViT-B/32) and its runtime deps — installed from source:
#   pip install git+https://github.com/openai/CLIP.git
clip==1.0
ftfy==6.3.1
regex==2025.11.3
